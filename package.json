{
  "name": "freshcart",
  "version": "1.0.0",
  "description": "Online grocery delivery system",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && cp -r public/* client/dist/ 2>/dev/null || true",
    "start": "node server/server.js",
    "dev": "nodemon server/server.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.19.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
